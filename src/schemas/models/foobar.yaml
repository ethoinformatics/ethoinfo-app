name: Foobar

# Which field will we use to display instances of this model in our app
displayField: sampleLabel

fields:
  - name: observers
    type: [Observer]
    lookup: true
  
  - name: colors
    type: [String]