---
# Model schema that passes validation
name: ModelPassExample
fields:
  - name: date
    type: Date # One of two special data types (Date, Geolocation)
  - name: foos
    type: [FooCategory] # Reference to user defined category type
  - name: bars
    type: [BarCategory] # Reference to user defined category type