import React from 'react';
import { observer } from 'mobx-react';
import { Page } from 'react-onsenui';

const CodeManager = observer(() => (
  <Page>
    Code Manager view goes here
  </Page>
));

export default CodeManager;
