import React from 'react';
import { observer } from 'mobx-react';
import { Page } from 'react-onsenui';

const GeoViewer = observer(() => (
  <Page>
    Geolocation view goes here
  </Page>
));

export default GeoViewer;
